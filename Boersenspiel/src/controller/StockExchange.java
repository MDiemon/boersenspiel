package controller;

public class StockExchange {

	public static void sellStock()//(Stock stock, Cost cst) oder ID
	{
		//TODO
		//Model.buyStock(User.getID, Stock.getID, cst);
		//User.setBalance(User.getBalance - cst);
	}
	
	public static void buyStock()//(Stock stock, Cost cst) oder ID
	{
		//TODO
		//Model.sellStock(User.getID, Stock.getID, cst);
		//User.setBalance(User.getBalance + cst);
	}
}
